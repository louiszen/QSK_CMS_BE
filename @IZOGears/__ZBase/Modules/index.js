const CouchDB = require('./Database/CouchDB/CouchDB');
const DesignDoc = require('./Database/CouchDB/DesignDocs');

/**
 * Generic Module for External Libraries
 */
module.exports = {
  CouchDB,
  DesignDoc
}