to${1} = (docID, doc, onQuit, onQuitRefresh, renderFormizo, addOns) => {
  return (
    <${1}
      docID={docID}
      doc={doc}
      onQuit={onQuit}
      onQuitRefresh={onQuitRefresh}
      renderFormizo={renderFormizo}
      addOns={addOns}
      />
  );
}